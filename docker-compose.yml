version: "3.8"

services:
  hardhat:
    build:
      context: .
      dockerfile: Dockerfile.hardhat
    container_name: skavenge-hardhat
    ports:
      - "8545:8545"
    networks:
      - skavenge-network
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8545"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s

  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    container_name: skavenge-test
    depends_on:
      hardhat:
        condition: service_healthy
    environment:
      - HARDHAT_URL=http://hardhat:8545
    networks:
      - skavenge-network

  deploy-contract:
    build:
      context: .
      dockerfile: Dockerfile.deploy-contract
    container_name: skavenge-deploy-contract
    restart: "no"
    depends_on:
      hardhat:
        condition: service_healthy
    environment:
      - HARDHAT_URL=http://hardhat:8545
      - GATEWAY_URL=http://gateway:4591
    volumes:
      - ./deploy-config.json:/app/deploy-config.json:ro
      - ./webapp/config.json:/app/webapp/config.json
    networks:
      - skavenge-network

  webapp:
    build:
      context: ./webapp
      dockerfile: Dockerfile
    container_name: skavenge-webapp
    depends_on:
      gateway:
        condition: service_healthy
    ports:
      - "8080:80"
    volumes:
      - ./webapp/config.json:/usr/share/nginx/html/config.json:ro
    networks:
      - skavenge-network

  gateway:
    build:
      context: .
      dockerfile: Dockerfile.gateway
    container_name: skavenge-gateway
    depends_on:
      deploy-contract:
        condition: service_completed_successfully
    ports:
      - "4591:4591"
    volumes:
      - ./webapp/config.json:/app/config.json:ro
    environment:
      - CONFIG_FILE=/app/config.json
    networks:
      - skavenge-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:4591/health"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s

  indexer:
    build:
      context: .
      dockerfile: Dockerfile.indexer
    container_name: skavenge-indexer
    depends_on:
      gateway:
        condition: service_healthy
    ports:
      - "4040:4040"
    volumes:
      - ./webapp/config.json:/app/webapp-config.json:ro
    networks:
      - skavenge-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:4040/health"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s

  mint-clues:
    build:
      context: .
      dockerfile: Dockerfile.mint-clues
    container_name: skavenge-mint-clues
    restart: "no"
    depends_on:
      gateway:
        condition: service_healthy
      indexer:
        condition: service_healthy
    environment:
      - HARDHAT_URL=http://hardhat:8545
      - GATEWAY_URL=http://gateway:4591
      - INDEXER_URL=http://indexer:4040
    volumes:
      - ./mint-config.json:/app/mint-config.json:ro
      - ./webapp/config.json:/app/webapp/config.json:ro
    networks:
      - skavenge-network

networks:
  skavenge-network:
    driver: bridge
